# Rustfmt configuration for sapo-cli
# This ensures consistent formatting across all environments

edition = "2021"
max_width = 100
tab_spaces = 4
newline_style = "Auto"
use_small_heuristics = "Default"
